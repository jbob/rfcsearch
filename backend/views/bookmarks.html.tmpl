% view.layout = 'layout';
<main>
  <h1>bookmarks</h1>
  <ul>
  % for (const bookmark of userBookmarks) {
    <li>
      <a href="/?q=<%= bookmark.number %>&queryBy=doc_id"><%= bookmark.number %></a> <span><%= bookmark.title %></span>
    </li>
  % }
  </ul>
</main>
